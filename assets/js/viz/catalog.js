/*
 * viz/catalog.js — 알고리즘 도감 카탈로그
 * 새 표본 추가 시: 모듈 파일 1개 + 여기 항목 1개만 추가하면 인덱스/상세가 자동 생성됨
 * code: { cpp, c, csharp, python, java } — 탭 순서도 이 키 순서를 따름
 * 각 언어 블록 안에 '표준 라이브러리' / '직접 구현' 구현을 주석으로 구분해 담음
 */
window.VIZ_CATALOG = [
  {
    id: 'insertion-sort',
    no: '001',
    title: 'Insertion Sort',
    ko: '삽입 정렬',
    category: 'Sorting',
    color: '#8fb8a0',
    complexity: 'O(n²)',
    note: '정렬된 앞부분에 새 원소를 하나씩 끼워 넣는다',
    best: 'O(n)',
    worst: 'O(n²)',
    tags: ['Stable', 'In-place'],
    extras: [
      {
        module: 'insertion-sort-cases',
        title: '입력 상태에 따른 비교 횟수',
        caption: '거의 정렬된 입력에서는 O(n)에 가깝게 끝나지만 역순 입력은 O(n²). 삽입 정렬이 적응형(adaptive)인 이유.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'void insertionSort(std::vector<int>& a) {',
        '  for (int i = 1; i < (int)a.size(); i++) {',
        '    // 정렬된 구간에서 삽입 위치를 탐색',
        '    auto pos = std::upper_bound(',
        '        a.begin(), a.begin() + i, a[i]);',
        '    // [pos, i] 구간을 한 칸 밀며 삽입',
        '    std::rotate(pos, a.begin() + i, a.begin() + i + 1);',
        '  }',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void insertionSort(std::vector<int>& a) {',
        '  for (int i = 1; i < (int)a.size(); i++) {',
        '    int key = a[i];',
        '    int j = i - 1;',
        '    while (j >= 0 && a[j] > key) {',
        '      a[j + 1] = a[j];',
        '      j--;',
        '    }',
        '    a[j + 1] = key;',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: memmove (string.h) ──',
        'void insertionSort(int a[], int n) {',
        '  for (int i = 1; i < n; i++) {',
        '    int key = a[i];',
        '    int j = i - 1;',
        '    while (j >= 0 && a[j] > key) j--;',
        '    // [j+1, i) 구간을 한 칸 뒤로 밀고 삽입',
        '    memmove(&a[j + 2], &a[j + 1],',
        '            (size_t)(i - j - 1) * sizeof(int));',
        '    a[j + 1] = key;',
        '  }',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void insertionSort(int a[], int n) {',
        '  for (int i = 1; i < n; i++) {',
        '    int key = a[i];',
        '    int j = i - 1;',
        '    while (j >= 0 && a[j] > key) {',
        '      a[j + 1] = a[j];',
        '      j--;',
        '    }',
        '    a[j + 1] = key;',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: List.BinarySearch + Insert ──',
        'void InsertionSort(List<int> a) {',
        '  for (int i = 1; i < a.Count; i++) {',
        '    int key = a[i];',
        '    // 정렬된 구간에서 삽입 위치를 탐색',
        '    int pos = a.GetRange(0, i).BinarySearch(key);',
        '    if (pos < 0) pos = ~pos; // 삽입 위치로 변환',
        '    a.RemoveAt(i);',
        '    a.Insert(pos, key);',
        '  }',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void InsertionSort(int[] a) {',
        '  for (int i = 1; i < a.Length; i++) {',
        '    int key = a[i];',
        '    int j = i - 1;',
        '    while (j >= 0 && a[j] > key) {',
        '      a[j + 1] = a[j];',
        '      j--;',
        '    }',
        '    a[j + 1] = key;',
        '  }',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'import bisect',
        '',
        'def insertion_sort(a):',
        '    for i in range(1, len(a)):',
        '        key = a.pop(i)',
        '        # 정렬된 구간 a[0:i]에 삽입 위치를 찾아 삽입',
        '        bisect.insort(a, key, lo=0, hi=i)',
        '',
        '# ── 직접 구현 ──',
        'def insertion_sort(a):',
        '    for i in range(1, len(a)):',
        '        key = a[i]',
        '        j = i - 1',
        '        while j >= 0 and a[j] > key:',
        '            a[j + 1] = a[j]',
        '            j -= 1',
        '        a[j + 1] = key'
      ],
      java: [
        '// ── 표준 라이브러리: Collections.binarySearch ──',
        'void insertionSort(List<Integer> a) {',
        '  for (int i = 1; i < a.size(); i++) {',
        '    int key = a.remove(i);',
        '    // 정렬된 구간에서 삽입 위치를 탐색',
        '    int pos = Collections.binarySearch(a.subList(0, i), key);',
        '    if (pos < 0) pos = -pos - 1; // 삽입 위치로 변환',
        '    a.add(pos, key);',
        '  }',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void insertionSort(int[] a) {',
        '  for (int i = 1; i < a.length; i++) {',
        '    int key = a[i];',
        '    int j = i - 1;',
        '    while (j >= 0 && a[j] > key) {',
        '      a[j + 1] = a[j];',
        '      j--;',
        '    }',
        '    a[j + 1] = key;',
        '  }',
        '}'
      ]
    }
  },
  {
    id: 'bfs-pathfinding',
    no: '002',
    title: 'Breadth-First Search',
    ko: '너비 우선 탐색',
    category: 'Graph',
    color: '#a9bcd0',
    complexity: 'O(V + E)',
    note: '시작점에서 물결처럼 퍼지며 탐색해 최단 경로를 찾는다',
    best: 'O(V + E)',
    worst: 'O(V + E)',
    tags: ['Shortest path', 'Unweighted'],
    extras: [
      {
        module: 'bfs-vs-dfs',
        title: '같은 미로, 다른 순서',
        caption: 'BFS는 넓게 퍼지고 DFS는 깊게 파고든다. 둘 다 모든 칸을 방문하지만 방문 순서가 다르다.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'std::vector<int> bfs(const Grid& g, int start, int goal) {',
        '  std::queue<int> q;',
        '  std::vector<int> cameFrom(g.size(), -1);',
        '  q.push(start);',
        '  cameFrom[start] = start;',
        '  while (!q.empty()) {',
        '    int cell = q.front(); q.pop();',
        '    if (cell == goal) break;',
        '    for (int next : g.neighbors(cell)) {',
        '      if (cameFrom[next] == -1) {',
        '        cameFrom[next] = cell;',
        '        q.push(next);',
        '      }',
        '    }',
        '  }',
        '  return cameFrom;',
        '}',
        '',
        '// ── 직접 구현: 배열 기반 큐 (g[y][x] == 1 이면 벽) ──',
        'void bfs(int g[][MAXC], int start, int goal, int cameFrom[]) {',
        '  int q[MAXC * MAXR], head = 0, tail = 0;',
        '  for (int i = 0; i < MAXC * MAXR; i++) cameFrom[i] = -1;',
        '  q[tail++] = start;',
        '  cameFrom[start] = start;',
        '  const int dx[4] = {-1, 1, 0, 0};',
        '  const int dy[4] = {0, 0, -1, 1};',
        '  while (head < tail) {',
        '    int cell = q[head++];',
        '    if (cell == goal) break;',
        '    int x = cell % MAXC, y = cell / MAXC;',
        '    for (int d = 0; d < 4; d++) {',
        '      int nx = x + dx[d], ny = y + dy[d];',
        '      if (nx < 0 || ny < 0 || nx >= MAXC || ny >= MAXR) continue;',
        '      int next = ny * MAXC + nx;',
        '      if (g[ny][nx] == 1 || cameFrom[next] != -1) continue;',
        '      cameFrom[next] = cell;',
        '      q[tail++] = next;',
        '    }',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: C 표준에는 큐가 없어 배열로 구현 ──',
        'void bfs(int g[][MAXC], int start, int goal, int cameFrom[]) {',
        '  int q[MAXC * MAXR], head = 0, tail = 0;',
        '  for (int i = 0; i < MAXC * MAXR; i++) cameFrom[i] = -1;',
        '  q[tail++] = start;',
        '  cameFrom[start] = start;',
        '  const int dx[4] = {-1, 1, 0, 0};',
        '  const int dy[4] = {0, 0, -1, 1};',
        '  while (head < tail) {',
        '    int cell = q[head++];',
        '    if (cell == goal) break;',
        '    int x = cell % MAXC, y = cell / MAXC;',
        '    for (int d = 0; d < 4; d++) {',
        '      int nx = x + dx[d], ny = y + dy[d];',
        '      if (nx < 0 || ny < 0 || nx >= MAXC || ny >= MAXR) continue;',
        '      int next = ny * MAXC + nx;',
        '      if (g[ny][nx] == 1 || cameFrom[next] != -1) continue;',
        '      cameFrom[next] = cell;',
        '      q[tail++] = next;',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 원형 버퍼 큐 ──',
        'void bfs(int g[][MAXC], int start, int goal, int cameFrom[]) {',
        '  int q[MAXC * MAXR], head = 0, tail = 0, count = 0;',
        '  for (int i = 0; i < MAXC * MAXR; i++) cameFrom[i] = -1;',
        '  q[tail] = start;',
        '  tail = (tail + 1) % (MAXC * MAXR);',
        '  count++;',
        '  cameFrom[start] = start;',
        '  const int dx[4] = {-1, 1, 0, 0};',
        '  const int dy[4] = {0, 0, -1, 1};',
        '  while (count > 0) {',
        '    int cell = q[head];',
        '    head = (head + 1) % (MAXC * MAXR);',
        '    count--;',
        '    if (cell == goal) break;',
        '    int x = cell % MAXC, y = cell / MAXC;',
        '    for (int d = 0; d < 4; d++) {',
        '      int nx = x + dx[d], ny = y + dy[d];',
        '      if (nx < 0 || ny < 0 || nx >= MAXC || ny >= MAXR) continue;',
        '      int next = ny * MAXC + nx;',
        '      if (g[ny][nx] == 1 || cameFrom[next] != -1) continue;',
        '      cameFrom[next] = cell;',
        '      q[tail] = next;',
        '      tail = (tail + 1) % (MAXC * MAXR);',
        '      count++;',
        '    }',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Queue<T> ──',
        'int[] Bfs(Grid g, int start, int goal) {',
        '  var q = new Queue<int>();',
        '  var cameFrom = Enumerable.Repeat(-1, g.Size).ToArray();',
        '  q.Enqueue(start);',
        '  cameFrom[start] = start;',
        '  while (q.Count > 0) {',
        '    int cell = q.Dequeue();',
        '    if (cell == goal) break;',
        '    foreach (int next in g.Neighbors(cell)) {',
        '      if (cameFrom[next] == -1) {',
        '        cameFrom[next] = cell;',
        '        q.Enqueue(next);',
        '      }',
        '    }',
        '  }',
        '  return cameFrom;',
        '}',
        '',
        '// ── 직접 구현: 배열 + head 인덱스 ──',
        'int[] Bfs(Grid g, int start, int goal) {',
        '  int[] q = new int[g.Size];',
        '  int head = 0, tail = 0;',
        '  var cameFrom = Enumerable.Repeat(-1, g.Size).ToArray();',
        '  q[tail++] = start;',
        '  cameFrom[start] = start;',
        '  while (head < tail) {',
        '    int cell = q[head++];',
        '    if (cell == goal) break;',
        '    foreach (int next in g.Neighbors(cell)) {',
        '      if (cameFrom[next] == -1) {',
        '        cameFrom[next] = cell;',
        '        q[tail++] = next;',
        '      }',
        '    }',
        '  }',
        '  return cameFrom;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'from collections import deque',
        '',
        'def bfs(grid, start, goal):',
        '    came_from = {start: None}',
        '    queue = deque([start])',
        '    while queue:',
        '        cell = queue.popleft()',
        '        if cell == goal:',
        '            break',
        '        for nxt in grid.neighbors(cell):',
        '            if nxt not in came_from:',
        '                came_from[nxt] = cell',
        '                queue.append(nxt)',
        '    return came_from',
        '',
        '# ── 직접 구현: 리스트 + head 인덱스 ──',
        'def bfs(grid, start, goal):',
        '    came_from = {start: None}',
        '    queue = [start]',
        '    head = 0',
        '    while head < len(queue):',
        '        cell = queue[head]',
        '        head += 1',
        '        if cell == goal:',
        '            break',
        '        for nxt in grid.neighbors(cell):',
        '            if nxt not in came_from:',
        '                came_from[nxt] = cell',
        '                queue.append(nxt)',
        '    return came_from'
      ],
      java: [
        '// ── 표준 라이브러리: ArrayDeque ──',
        'int[] bfs(Grid g, int start, int goal) {',
        '  Queue<Integer> q = new ArrayDeque<>();',
        '  int[] cameFrom = new int[g.size()];',
        '  Arrays.fill(cameFrom, -1);',
        '  q.add(start);',
        '  cameFrom[start] = start;',
        '  while (!q.isEmpty()) {',
        '    int cell = q.poll();',
        '    if (cell == goal) break;',
        '    for (int next : g.neighbors(cell)) {',
        '      if (cameFrom[next] == -1) {',
        '        cameFrom[next] = cell;',
        '        q.add(next);',
        '      }',
        '    }',
        '  }',
        '  return cameFrom;',
        '}',
        '',
        '// ── 직접 구현: 배열 + head 인덱스 ──',
        'int[] bfs(Grid g, int start, int goal) {',
        '  int[] q = new int[g.size()];',
        '  int head = 0, tail = 0;',
        '  int[] cameFrom = new int[g.size()];',
        '  Arrays.fill(cameFrom, -1);',
        '  q[tail++] = start;',
        '  cameFrom[start] = start;',
        '  while (head < tail) {',
        '    int cell = q[head++];',
        '    if (cell == goal) break;',
        '    for (int next : g.neighbors(cell)) {',
        '      if (cameFrom[next] == -1) {',
        '        cameFrom[next] = cell;',
        '        q[tail++] = next;',
        '      }',
        '    }',
        '  }',
        '  return cameFrom;',
        '}'
      ]
    }
  },
  {
    id: 'binary-search',
    no: '003',
    title: 'Binary Search',
    ko: '이진 탐색',
    category: 'Searching',
    color: '#cfa652',
    complexity: 'O(log n)',
    note: '정렬된 배열에서 탐색 범위를 매번 절반으로 줄인다',
    best: 'O(1)',
    worst: 'O(log n)',
    tags: ['Sorted input', 'Halving'],
    extras: [
      {
        module: 'binary-vs-linear',
        title: '선형 탐색과의 비교 횟수',
        caption: '같은 목표를 찾는 데 선형은 최대 n번, 이진은 최대 log n번. 정렬되어 있다는 전제가 값을 한다.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'int binarySearch(const std::vector<int>& a, int target) {',
        '  auto it = std::lower_bound(a.begin(), a.end(), target);',
        '  if (it != a.end() && *it == target)',
        '    return (int)(it - a.begin());',
        '  return -1;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int binarySearch(const std::vector<int>& a, int target) {',
        '  int lo = 0, hi = (int)a.size() - 1;',
        '  while (lo <= hi) {',
        '    int mid = lo + (hi - lo) / 2;',
        '    if (a[mid] == target) return mid;',
        '    if (a[mid] < target) lo = mid + 1;',
        '    else hi = mid - 1;',
        '  }',
        '  return -1;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: bsearch (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'int binarySearch(const int a[], int n, int target) {',
        '  const int* p = bsearch(&target, a, n, sizeof(int), cmpAsc);',
        '  return p ? (int)(p - a) : -1;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int binarySearch(const int a[], int n, int target) {',
        '  int lo = 0, hi = n - 1;',
        '  while (lo <= hi) {',
        '    int mid = lo + (hi - lo) / 2;',
        '    if (a[mid] == target) return mid;',
        '    if (a[mid] < target) lo = mid + 1;',
        '    else hi = mid - 1;',
        '  }',
        '  return -1;',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.BinarySearch ──',
        'int BinarySearch(int[] a, int target) {',
        '  int idx = Array.BinarySearch(a, target);',
        '  return idx >= 0 ? idx : -1;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int BinarySearch(int[] a, int target) {',
        '  int lo = 0, hi = a.Length - 1;',
        '  while (lo <= hi) {',
        '    int mid = lo + (hi - lo) / 2;',
        '    if (a[mid] == target) return mid;',
        '    if (a[mid] < target) lo = mid + 1;',
        '    else hi = mid - 1;',
        '  }',
        '  return -1;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'import bisect',
        '',
        'def binary_search(a, target):',
        '    i = bisect.bisect_left(a, target)',
        '    if i < len(a) and a[i] == target:',
        '        return i',
        '    return -1',
        '',
        '# ── 직접 구현 ──',
        'def binary_search(a, target):',
        '    lo, hi = 0, len(a) - 1',
        '    while lo <= hi:',
        '        mid = (lo + hi) // 2',
        '        if a[mid] == target:',
        '            return mid',
        '        if a[mid] < target:',
        '            lo = mid + 1',
        '        else:',
        '            hi = mid - 1',
        '    return -1'
      ],
      java: [
        '// ── 표준 라이브러리: Arrays.binarySearch ──',
        'int binarySearch(int[] a, int target) {',
        '  int idx = Arrays.binarySearch(a, target);',
        '  return idx >= 0 ? idx : -1;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int binarySearch(int[] a, int target) {',
        '  int lo = 0, hi = a.length - 1;',
        '  while (lo <= hi) {',
        '    int mid = lo + (hi - lo) / 2;',
        '    if (a[mid] == target) return mid;',
        '    if (a[mid] < target) lo = mid + 1;',
        '    else hi = mid - 1;',
        '  }',
        '  return -1;',
        '}'
      ]
    }
  },
  {
    id: 'selection-sort',
    no: '004',
    title: 'Selection Sort',
    ko: '선택 정렬',
    category: 'Sorting',
    color: '#dfa392',
    complexity: 'O(n²)',
    note: '남은 구간에서 최솟값을 골라 앞으로 보낸다',
    best: 'O(n²)',
    worst: 'O(n²)',
    tags: ['In-place', 'Unstable'],
    extras: [
      {
        module: 'selection-sort-swaps',
        title: '버블 정렬과의 교환 횟수 비교',
        caption: '같은 입력에서 선택 정렬의 교환은 최대 n-1번. 쓰기 비용이 큰 환경(플래시 메모리 등)에서 선택 정렬이 쓰이는 이유.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'void selectionSort(std::vector<int>& a) {',
        '  for (int i = 0; i < (int)a.size(); i++) {',
        '    // [i, 끝) 구간의 최솟값 위치를 탐색',
        '    auto minIt = std::min_element(a.begin() + i, a.end());',
        '    std::swap(a[i], *minIt);',
        '  }',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void selectionSort(std::vector<int>& a) {',
        '  for (int i = 0; i < (int)a.size(); i++) {',
        '    int minIdx = i;',
        '    for (int j = i + 1; j < (int)a.size(); j++) {',
        '      if (a[j] < a[minIdx]) minIdx = j;',
        '    }',
        '    int t = a[i];',
        '    a[i] = a[minIdx];',
        '    a[minIdx] = t;',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void selectionSort(int a[], int n) {',
        '  qsort(a, n, sizeof(int), cmpAsc); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void selectionSort(int a[], int n) {',
        '  for (int i = 0; i < n - 1; i++) {',
        '    int minIdx = i;',
        '    for (int j = i + 1; j < n; j++) {',
        '      if (a[j] < a[minIdx]) minIdx = j;',
        '    }',
        '    int t = a[i];',
        '    a[i] = a[minIdx];',
        '    a[minIdx] = t;',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.Sort (Introsort) ──',
        'void SelectionSort(int[] a) {',
        '  Array.Sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void SelectionSort(int[] a) {',
        '  for (int i = 0; i < a.Length - 1; i++) {',
        '    int minIdx = i;',
        '    for (int j = i + 1; j < a.Length; j++) {',
        '      if (a[j] < a[minIdx]) minIdx = j;',
        '    }',
        '    int t = a[i];',
        '    a[i] = a[minIdx];',
        '    a[minIdx] = t;',
        '  }',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'def selection_sort(a):',
        '    for i in range(len(a)):',
        '        # a[i:]의 최솟값 위치를 찾아 교환',
        '        j = a.index(min(a[i:]), i)',
        '        a[i], a[j] = a[j], a[i]',
        '',
        '# ── 직접 구현 ──',
        'def selection_sort(a):',
        '    for i in range(len(a)):',
        '        min_idx = i',
        '        for j in range(i + 1, len(a)):',
        '            if a[j] < a[min_idx]:',
        '                min_idx = j',
        '        a[i], a[min_idx] = a[min_idx], a[i]'
      ],
      java: [
        '// ── 표준 라이브러리: Arrays.sort (Dual-Pivot Quicksort) ──',
        'void selectionSort(int[] a) {',
        '  Arrays.sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void selectionSort(int[] a) {',
        '  for (int i = 0; i < a.length - 1; i++) {',
        '    int minIdx = i;',
        '    for (int j = i + 1; j < a.length; j++) {',
        '      if (a[j] < a[minIdx]) minIdx = j;',
        '    }',
        '    int t = a[i];',
        '    a[i] = a[minIdx];',
        '    a[minIdx] = t;',
        '  }',
        '}'
      ]
    }
  },
  {
    id: 'linear-search',
    no: '005',
    title: 'Linear Search',
    ko: '선형 탐색',
    category: 'Searching',
    color: '#a89cc8',
    complexity: 'O(n)',
    note: '처음부터 끝까지 하나씩 순서대로 비교한다',
    best: 'O(1)',
    worst: 'O(n)',
    tags: ['Unsorted OK', 'Sequential'],
    extras: [
      {
        module: 'linear-search-cases',
        title: '목표 위치에 따른 비교 횟수',
        caption: '맨 앞이면 1번, 맨 끝이면 n번. 평균적으로는 절반을 훑는다.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'int linearSearch(const std::vector<int>& a, int target) {',
        '  auto it = std::find(a.begin(), a.end(), target);',
        '  return it != a.end() ? (int)(it - a.begin()) : -1;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int linearSearch(const std::vector<int>& a, int target) {',
        '  for (int i = 0; i < (int)a.size(); i++) {',
        '    if (a[i] == target) return i;',
        '  }',
        '  return -1;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: 전용 함수가 없어 인덱스 순회 ──',
        'int linearSearch(const int a[], int n, int target) {',
        '  for (int i = 0; i < n; i++) {',
        '    if (a[i] == target) return i;',
        '  }',
        '  return -1;',
        '}',
        '',
        '// ── 직접 구현: 포인터 순회 ──',
        'int linearSearch(const int* a, int n, int target) {',
        '  const int* end = a + n;',
        '  for (const int* p = a; p < end; p++) {',
        '    if (*p == target) return (int)(p - a);',
        '  }',
        '  return -1;',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.IndexOf ──',
        'int LinearSearch(int[] a, int target) {',
        '  return Array.IndexOf(a, target); // 없으면 -1',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int LinearSearch(int[] a, int target) {',
        '  for (int i = 0; i < a.Length; i++) {',
        '    if (a[i] == target) return i;',
        '  }',
        '  return -1;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'def linear_search(a, target):',
        '    if target in a:',
        '        return a.index(target)',
        '    return -1',
        '',
        '# ── 직접 구현 ──',
        'def linear_search(a, target):',
        '    for i, v in enumerate(a):',
        '        if v == target:',
        '            return i',
        '    return -1'
      ],
      java: [
        '// ── 표준 라이브러리: IntStream ──',
        'int linearSearch(int[] a, int target) {',
        '  return IntStream.range(0, a.length)',
        '      .filter(i -> a[i] == target)',
        '      .findFirst()',
        '      .orElse(-1);',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int linearSearch(int[] a, int target) {',
        '  for (int i = 0; i < a.length; i++) {',
        '    if (a[i] == target) return i;',
        '  }',
        '  return -1;',
        '}'
      ]
    }
  },
  {
    id: 'bubble-sort',
    no: '006',
    title: 'Bubble Sort',
    ko: '버블 정렬',
    category: 'Sorting',
    color: '#8fa8b8',
    complexity: 'O(n²)',
    note: '인접한 두 원소를 비교·교환하며 큰 값을 뒤로 띄운다',
    best: 'O(n)',
    worst: 'O(n²)',
    tags: ['Stable', 'In-place'],
    extras: [
      {
        module: 'bubble-sort-early-exit',
        title: '조기 종료의 효과',
        caption: '거의 정렬된 입력에서 한 패스에 교환이 없으면 바로 종료. 최선의 경우 O(n)이 되는 이유.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 ──',
        'void bubbleSort(std::vector<int>& a) {',
        '  for (int end = (int)a.size() - 1; end > 0; end--) {',
        '    for (int j = 0; j < end; j++) {',
        '      if (a[j] > a[j + 1])',
        '        std::swap(a[j], a[j + 1]);',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현 (조기 종료 포함) ──',
        'void bubbleSort(std::vector<int>& a) {',
        '  for (int end = (int)a.size() - 1; end > 0; end--) {',
        '    bool swapped = false;',
        '    for (int j = 0; j < end; j++) {',
        '      if (a[j] > a[j + 1]) {',
        '        int t = a[j];',
        '        a[j] = a[j + 1];',
        '        a[j + 1] = t;',
        '        swapped = true;',
        '      }',
        '    }',
        '    if (!swapped) break; // 교환이 없으면 정렬 완료',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void bubbleSort(int a[], int n) {',
        '  qsort(a, n, sizeof(int), cmpAsc); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 (조기 종료 포함) ──',
        'void bubbleSort(int a[], int n) {',
        '  for (int end = n - 1; end > 0; end--) {',
        '    int swapped = 0;',
        '    for (int j = 0; j < end; j++) {',
        '      if (a[j] > a[j + 1]) {',
        '        int t = a[j];',
        '        a[j] = a[j + 1];',
        '        a[j + 1] = t;',
        '        swapped = 1;',
        '      }',
        '    }',
        '    if (!swapped) break; // 교환이 없으면 정렬 완료',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.Sort (Introsort) ──',
        'void BubbleSort(int[] a) {',
        '  Array.Sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 (조기 종료 포함) ──',
        'void BubbleSort(int[] a) {',
        '  for (int end = a.Length - 1; end > 0; end--) {',
        '    bool swapped = false;',
        '    for (int j = 0; j < end; j++) {',
        '      if (a[j] > a[j + 1]) {',
        '        int t = a[j];',
        '        a[j] = a[j + 1];',
        '        a[j + 1] = t;',
        '        swapped = true;',
        '      }',
        '    }',
        '    if (!swapped) break; // 교환이 없으면 정렬 완료',
        '  }',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리 ──',
        'def bubble_sort(a):',
        '    for end in range(len(a) - 1, 0, -1):',
        '        for j in range(end):',
        '            if a[j] > a[j + 1]:',
        '                a[j], a[j + 1] = a[j + 1], a[j]',
        '',
        '# ── 직접 구현 (조기 종료 포함) ──',
        'def bubble_sort(a):',
        '    for end in range(len(a) - 1, 0, -1):',
        '        swapped = False',
        '        for j in range(end):',
        '            if a[j] > a[j + 1]:',
        '                t = a[j]',
        '                a[j] = a[j + 1]',
        '                a[j + 1] = t',
        '                swapped = True',
        '        if not swapped:',
        '            break  # 교환이 없으면 정렬 완료'
      ],
      java: [
        '// ── 표준 라이브러리: Arrays.sort (Dual-Pivot Quicksort) ──',
        'void bubbleSort(int[] a) {',
        '  Arrays.sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현 (조기 종료 포함) ──',
        'void bubbleSort(int[] a) {',
        '  for (int end = a.length - 1; end > 0; end--) {',
        '    boolean swapped = false;',
        '    for (int j = 0; j < end; j++) {',
        '      if (a[j] > a[j + 1]) {',
        '        int t = a[j];',
        '        a[j] = a[j + 1];',
        '        a[j + 1] = t;',
        '        swapped = true;',
        '      }',
        '    }',
        '    if (!swapped) break; // 교환이 없으면 정렬 완료',
        '  }',
        '}'
      ]
    }
  },
  {
    id: 'dfs-graph',
    no: '007',
    title: 'Depth-First Search',
    ko: '깊이 우선 탐색',
    category: 'Graph',
    color: '#8f96c4',
    complexity: 'O(V + E)',
    note: '갈 수 있는 곳까지 깊이 내려간 뒤 되돌아오며 탐색한다',
    best: 'O(V + E)',
    worst: 'O(V + E)',
    tags: ['Stack', 'Backtracking'],
    extras: [
      {
        module: 'dfs-stack',
        title: '스택이 기억하는 되돌아갈 길',
        caption: '갈림길을 스택에 쌓고, 막다른 길에서 꺼내며 되돌아온다. 재귀는 이 스택을 호출 스택으로 대신한 것.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::stack (반복) ──',
        'void dfs(const Graph& g, int start) {',
        '  std::vector<bool> visited(g.size(), false);',
        '  std::stack<int> s;',
        '  s.push(start);',
        '  while (!s.empty()) {',
        '    int v = s.top(); s.pop();',
        '    if (visited[v]) continue;',
        '    visited[v] = true;',
        '    for (int next : g.neighbors(v)) {',
        '      if (!visited[next]) s.push(next);',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 재귀 ──',
        'void dfs(const Graph& g, int v, std::vector<bool>& visited) {',
        '  visited[v] = true;',
        '  for (int next : g.neighbors(v)) {',
        '    if (!visited[next]) dfs(g, next, visited);',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: C 표준에는 스택이 없어 배열로 구현 ──',
        'void dfs(int g[][MAXV], int n, int start) {',
        '  int stack[MAXV], top = -1;',
        '  int visited[MAXV] = {0};',
        '  stack[++top] = start;',
        '  while (top >= 0) {',
        '    int v = stack[top--];',
        '    if (visited[v]) continue;',
        '    visited[v] = 1;',
        '    for (int next = 0; next < n; next++) {',
        '      if (g[v][next] && !visited[next]) stack[++top] = next;',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 재귀 ──',
        'void dfs(int g[][MAXV], int n, int v, int visited[]) {',
        '  visited[v] = 1;',
        '  for (int next = 0; next < n; next++) {',
        '    if (g[v][next] && !visited[next]) dfs(g, n, next, visited);',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Stack<T> (반복) ──',
        'void Dfs(Graph g, int start) {',
        '  var visited = new bool[g.Size];',
        '  var s = new Stack<int>();',
        '  s.Push(start);',
        '  while (s.Count > 0) {',
        '    int v = s.Pop();',
        '    if (visited[v]) continue;',
        '    visited[v] = true;',
        '    foreach (int next in g.Neighbors(v)) {',
        '      if (!visited[next]) s.Push(next);',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 재귀 ──',
        'void Dfs(Graph g, int v, bool[] visited) {',
        '  visited[v] = true;',
        '  foreach (int next in g.Neighbors(v)) {',
        '    if (!visited[next]) Dfs(g, next, visited);',
        '  }',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: 리스트가 곧 스택 (반복) ──',
        'def dfs(graph, start):',
        '    visited = set()',
        '    stack = [start]',
        '    while stack:',
        '        v = stack.pop()',
        '        if v in visited:',
        '            continue',
        '        visited.add(v)',
        '        for nxt in graph.neighbors(v):',
        '            if nxt not in visited:',
        '                stack.append(nxt)',
        '',
        '# ── 직접 구현: 재귀 ──',
        'def dfs(graph, v, visited=None):',
        '    if visited is None:',
        '        visited = set()',
        '    visited.add(v)',
        '    for nxt in graph.neighbors(v):',
        '        if nxt not in visited:',
        '            dfs(graph, nxt, visited)'
      ],
      java: [
        '// ── 표준 라이브러리: ArrayDeque를 스택으로 (반복) ──',
        'void dfs(Graph g, int start) {',
        '  boolean[] visited = new boolean[g.size()];',
        '  ArrayDeque<Integer> s = new ArrayDeque<>();',
        '  s.push(start);',
        '  while (!s.isEmpty()) {',
        '    int v = s.pop();',
        '    if (visited[v]) continue;',
        '    visited[v] = true;',
        '    for (int next : g.neighbors(v)) {',
        '      if (!visited[next]) s.push(next);',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 재귀 ──',
        'void dfs(Graph g, int v, boolean[] visited) {',
        '  visited[v] = true;',
        '  for (int next : g.neighbors(v)) {',
        '    if (!visited[next]) dfs(g, next, visited);',
        '  }',
        '}'
      ]
    }
  },
  {
    id: 'merge-sort',
    no: '008',
    title: 'Merge Sort',
    ko: '병합 정렬',
    category: 'Sorting',
    color: '#a8b87f',
    complexity: 'O(n log n)',
    note: '절반으로 나누고, 정렬된 두 부분을 합치며 올라온다',
    best: 'O(n log n)',
    worst: 'O(n log n)',
    tags: ['Stable', 'Divide & conquer'],
    extras: [
      {
        module: 'merge-sort-tree',
        title: '분할과 병합의 트리',
        caption: '8개 원소 기준. 위로 내려가며 절반씩 나누고(분할), 아래에서 위로 합쳐진다(병합). 트리의 높이가 log n.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::merge ──',
        'void mergeSort(std::vector<int>& a, int lo, int hi) {',
        '  if (hi - lo <= 1) return;',
        '  int mid = lo + (hi - lo) / 2;',
        '  mergeSort(a, lo, mid);',
        '  mergeSort(a, mid, hi);',
        '  std::vector<int> buf(hi - lo);',
        '  std::merge(a.begin() + lo, a.begin() + mid,',
        '             a.begin() + mid, a.begin() + hi, buf.begin());',
        '  std::copy(buf.begin(), buf.end(), a.begin() + lo);',
        '}',
        '',
        '// ── 직접 구현: 두 포인터 병합 ──',
        'void mergeSort(std::vector<int>& a, std::vector<int>& buf,',
        '               int lo, int hi) {',
        '  if (hi - lo <= 1) return;',
        '  int mid = lo + (hi - lo) / 2;',
        '  mergeSort(a, buf, lo, mid);',
        '  mergeSort(a, buf, mid, hi);',
        '  int i = lo, j = mid, k = lo;',
        '  while (i < mid && j < hi)',
        '    buf[k++] = (a[i] <= a[j]) ? a[i++] : a[j++];',
        '  while (i < mid) buf[k++] = a[i++];',
        '  while (j < hi) buf[k++] = a[j++];',
        '  for (k = lo; k < hi; k++) a[k] = buf[k];',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void mergeSort(int a[], int n) {',
        '  qsort(a, n, sizeof(int), cmpAsc); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: 두 포인터 병합 ──',
        'void mergeSort(int a[], int buf[], int lo, int hi) {',
        '  if (hi - lo <= 1) return;',
        '  int mid = lo + (hi - lo) / 2;',
        '  mergeSort(a, buf, lo, mid);',
        '  mergeSort(a, buf, mid, hi);',
        '  int i = lo, j = mid, k = lo;',
        '  while (i < mid && j < hi)',
        '    buf[k++] = (a[i] <= a[j]) ? a[i++] : a[j++];',
        '  while (i < mid) buf[k++] = a[i++];',
        '  while (j < hi) buf[k++] = a[j++];',
        '  for (k = lo; k < hi; k++) a[k] = buf[k];',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.Sort (Introsort) ──',
        'void MergeSort(int[] a) {',
        '  Array.Sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: 두 포인터 병합 ──',
        'void MergeSort(int[] a, int[] buf, int lo, int hi) {',
        '  if (hi - lo <= 1) return;',
        '  int mid = lo + (hi - lo) / 2;',
        '  MergeSort(a, buf, lo, mid);',
        '  MergeSort(a, buf, mid, hi);',
        '  int i = lo, j = mid, k = lo;',
        '  while (i < mid && j < hi)',
        '    buf[k++] = (a[i] <= a[j]) ? a[i++] : a[j++];',
        '  while (i < mid) buf[k++] = a[i++];',
        '  while (j < hi) buf[k++] = a[j++];',
        '  for (k = lo; k < hi; k++) a[k] = buf[k];',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: heapq.merge ──',
        'import heapq',
        '',
        'def merge_sort(a):',
        '    if len(a) <= 1:',
        '        return a',
        '    mid = len(a) // 2',
        '    left = merge_sort(a[:mid])',
        '    right = merge_sort(a[mid:])',
        '    return list(heapq.merge(left, right))',
        '',
        '# ── 직접 구현: 두 포인터 병합 ──',
        'def merge_sort(a):',
        '    if len(a) <= 1:',
        '        return a',
        '    mid = len(a) // 2',
        '    left = merge_sort(a[:mid])',
        '    right = merge_sort(a[mid:])',
        '    merged = []',
        '    i = j = 0',
        '    while i < len(left) and j < len(right):',
        '        if left[i] <= right[j]:',
        '            merged.append(left[i])',
        '            i += 1',
        '        else:',
        '            merged.append(right[j])',
        '            j += 1',
        '    return merged + left[i:] + right[j:]'
      ],
      java: [
        '// ── 표준 라이브러리: Arrays.sort (Dual-Pivot Quicksort) ──',
        'void mergeSort(int[] a) {',
        '  Arrays.sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: 두 포인터 병합 ──',
        'void mergeSort(int[] a, int[] buf, int lo, int hi) {',
        '  if (hi - lo <= 1) return;',
        '  int mid = lo + (hi - lo) / 2;',
        '  mergeSort(a, buf, lo, mid);',
        '  mergeSort(a, buf, mid, hi);',
        '  int i = lo, j = mid, k = lo;',
        '  while (i < mid && j < hi)',
        '    buf[k++] = (a[i] <= a[j]) ? a[i++] : a[j++];',
        '  while (i < mid) buf[k++] = a[i++];',
        '  while (j < hi) buf[k++] = a[j++];',
        '  for (k = lo; k < hi; k++) a[k] = buf[k];',
        '}'
      ]
    }
  },
  {
    id: 'quick-sort',
    no: '009',
    title: 'Quick Sort',
    ko: '퀵 정렬',
    category: 'Sorting',
    color: '#c48f6f',
    complexity: 'O(n log n)',
    note: '피벗을 기준으로 작은 값과 큰 값을 나누며 내려간다',
    best: 'O(n log n)',
    worst: 'O(n²)',
    tags: ['In-place', 'Divide & conquer', 'Unstable'],
    extras: [
      {
        module: 'quick-sort-pivot',
        title: '피벗이 운명을 가른다',
        caption: '무작위 입력에서는 평균 O(n log n)이지만, 정렬된 입력에 끝 원소를 피벗으로 쓰면 매번 한쪽만 남아 O(n²). 실무 구현이 중앙값/무작위 피벗을 쓰는 이유.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::sort (Introsort) ──',
        'void quickSort(std::vector<int>& a) {',
        '  std::sort(a.begin(), a.end()); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: Lomuto 분할 ──',
        'void quickSort(std::vector<int>& a, int lo, int hi) {',
        '  if (lo >= hi) return;',
        '  int pivot = a[hi];',
        '  int i = lo;',
        '  for (int j = lo; j < hi; j++) {',
        '    if (a[j] < pivot) {',
        '      int t = a[i]; a[i] = a[j]; a[j] = t;',
        '      i++;',
        '    }',
        '  }',
        '  int t = a[i]; a[i] = a[hi]; a[hi] = t;',
        '  quickSort(a, lo, i - 1);',
        '  quickSort(a, i + 1, hi);',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void quickSort(int a[], int n) {',
        '  qsort(a, n, sizeof(int), cmpAsc); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: Lomuto 분할 ──',
        'void quickSort(int a[], int lo, int hi) {',
        '  if (lo >= hi) return;',
        '  int pivot = a[hi];',
        '  int i = lo;',
        '  for (int j = lo; j < hi; j++) {',
        '    if (a[j] < pivot) {',
        '      int t = a[i]; a[i] = a[j]; a[j] = t;',
        '      i++;',
        '    }',
        '  }',
        '  int t = a[i]; a[i] = a[hi]; a[hi] = t;',
        '  quickSort(a, lo, i - 1);',
        '  quickSort(a, i + 1, hi);',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: Array.Sort (Introsort) ──',
        'void QuickSort(int[] a) {',
        '  Array.Sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: Lomuto 분할 ──',
        'void QuickSort(int[] a, int lo, int hi) {',
        '  if (lo >= hi) return;',
        '  int pivot = a[hi];',
        '  int i = lo;',
        '  for (int j = lo; j < hi; j++) {',
        '    if (a[j] < pivot) {',
        '      int t = a[i]; a[i] = a[j]; a[j] = t;',
        '      i++;',
        '    }',
        '  }',
        '  int t = a[i]; a[i] = a[hi]; a[hi] = t;',
        '  QuickSort(a, lo, i - 1);',
        '  QuickSort(a, i + 1, hi);',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: sorted (Timsort) ──',
        'def quick_sort(a):',
        '    a[:] = sorted(a)  # 실무에서는 내장 정렬',
        '',
        '# ── 직접 구현: Lomuto 분할 ──',
        'def quick_sort(a, lo=0, hi=None):',
        '    if hi is None:',
        '        hi = len(a) - 1',
        '    if lo >= hi:',
        '        return',
        '    pivot = a[hi]',
        '    i = lo',
        '    for j in range(lo, hi):',
        '        if a[j] < pivot:',
        '            a[i], a[j] = a[j], a[i]',
        '            i += 1',
        '    a[i], a[hi] = a[hi], a[i]',
        '    quick_sort(a, lo, i - 1)',
        '    quick_sort(a, i + 1, hi)'
      ],
      java: [
        '// ── 표준 라이브러리: Arrays.sort (Dual-Pivot Quicksort) ──',
        'void quickSort(int[] a) {',
        '  Arrays.sort(a); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: Lomuto 분할 ──',
        'void quickSort(int[] a, int lo, int hi) {',
        '  if (lo >= hi) return;',
        '  int pivot = a[hi];',
        '  int i = lo;',
        '  for (int j = lo; j < hi; j++) {',
        '    if (a[j] < pivot) {',
        '      int t = a[i]; a[i] = a[j]; a[j] = t;',
        '      i++;',
        '    }',
        '  }',
        '  int t = a[i]; a[i] = a[hi]; a[hi] = t;',
        '  quickSort(a, lo, i - 1);',
        '  quickSort(a, i + 1, hi);',
        '}'
      ]
    }
  },
  {
    id: 'dijkstra',
    no: '010',
    title: 'Dijkstra',
    ko: '다익스트라',
    category: 'Graph',
    color: '#c47f9e',
    complexity: 'O((V + E) log V)',
    note: '가중치가 있는 그래프에서 누적 비용이 가장 작은 경로를 찾는다',
    best: 'O((V + E) log V)',
    worst: 'O((V + E) log V)',
    tags: ['Shortest path', 'Weighted', 'Greedy'],
    extras: [
      {
        module: 'dijkstra-vs-bfs',
        title: 'BFS와 무엇이 다른가',
        caption: 'BFS는 칸 수만 세기 때문에 진흙(비용 5)을 통과하는 지름길을 택하지만, Dijkstra는 돌아가더라도 총비용이 작은 길을 찾는다.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::priority_queue ──',
        'std::vector<int> dijkstra(const Graph& g, int start) {',
        '  const int INF = 1e9;',
        '  std::vector<int> dist(g.size(), INF);',
        '  using P = std::pair<int, int>; // (비용, 정점)',
        '  std::priority_queue<P, std::vector<P>, std::greater<P>> pq;',
        '  dist[start] = 0;',
        '  pq.push({0, start});',
        '  while (!pq.empty()) {',
        '    auto [d, v] = pq.top(); pq.pop();',
        '    if (d > dist[v]) continue; // 이미 더 좋은 경로로 확정됨',
        '    for (auto [next, w] : g.neighbors(v)) {',
        '      if (dist[v] + w < dist[next]) {',
        '        dist[next] = dist[v] + w;',
        '        pq.push({dist[next], next});',
        '      }',
        '    }',
        '  }',
        '  return dist;',
        '}',
        '',
        '// ── 직접 구현: 최솟값 선형 탐색 O(V²) ──',
        'std::vector<int> dijkstra(const Graph& g, int start) {',
        '  const int INF = 1e9;',
        '  std::vector<int> dist(g.size(), INF);',
        '  std::vector<bool> done(g.size(), false);',
        '  dist[start] = 0;',
        '  for (int iter = 0; iter < g.size(); iter++) {',
        '    int v = -1; // 미확정 정점 중 최소 비용',
        '    for (int i = 0; i < g.size(); i++) {',
        '      if (!done[i] && (v == -1 || dist[i] < dist[v])) v = i;',
        '    }',
        '    if (v == -1 || dist[v] == INF) break;',
        '    done[v] = true;',
        '    for (auto [next, w] : g.neighbors(v)) {',
        '      if (dist[v] + w < dist[next]) dist[next] = dist[v] + w;',
        '    }',
        '  }',
        '  return dist;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: C 표준에는 우선순위 큐가 없어 선형 탐색 ──',
        'void dijkstra(int g[][MAXV], int n, int start, int dist[]) {',
        '  int done[MAXV] = {0};',
        '  for (int i = 0; i < n; i++) dist[i] = INF;',
        '  dist[start] = 0;',
        '  for (int iter = 0; iter < n; iter++) {',
        '    int v = -1;',
        '    for (int i = 0; i < n; i++) {',
        '      if (!done[i] && (v == -1 || dist[i] < dist[v])) v = i;',
        '    }',
        '    if (v == -1 || dist[v] == INF) break;',
        '    done[v] = 1;',
        '    for (int next = 0; next < n; next++) {',
        '      int w = g[v][next];',
        '      if (w > 0 && dist[v] + w < dist[next])',
        '        dist[next] = dist[v] + w;',
        '    }',
        '  }',
        '}',
        '',
        '// ── 직접 구현: 이진 힙 우선순위 큐 ──',
        'typedef struct { int v, d; } Item;',
        'Item heap[MAXV * 4];',
        'int hsize = 0;',
        '',
        'void hpush(int v, int d) {',
        '  int i = hsize++;',
        '  heap[i] = (Item){v, d};',
        '  while (i > 0) {',
        '    int p = (i - 1) / 2;',
        '    if (heap[p].d <= heap[i].d) break;',
        '    Item t = heap[p]; heap[p] = heap[i]; heap[i] = t;',
        '    i = p;',
        '  }',
        '}',
        '',
        'Item hpop(void) {',
        '  Item top = heap[0];',
        '  heap[0] = heap[--hsize];',
        '  int i = 0;',
        '  for (;;) {',
        '    int l = i * 2 + 1, r = l + 1, m = i;',
        '    if (l < hsize && heap[l].d < heap[m].d) m = l;',
        '    if (r < hsize && heap[r].d < heap[m].d) m = r;',
        '    if (m == i) break;',
        '    Item t = heap[m]; heap[m] = heap[i]; heap[i] = t;',
        '    i = m;',
        '  }',
        '  return top;',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: PriorityQueue<TElement, TPriority> ──',
        'int[] Dijkstra(Graph g, int start) {',
        '  var dist = Enumerable.Repeat(int.MaxValue, g.Size).ToArray();',
        '  var pq = new PriorityQueue<int, int>();',
        '  dist[start] = 0;',
        '  pq.Enqueue(start, 0);',
        '  while (pq.Count > 0) {',
        '    int v = pq.Dequeue();',
        '    foreach ((int next, int w) in g.Neighbors(v)) {',
        '      if (dist[v] != int.MaxValue && dist[v] + w < dist[next]) {',
        '        dist[next] = dist[v] + w;',
        '        pq.Enqueue(next, dist[next]);',
        '      }',
        '    }',
        '  }',
        '  return dist;',
        '}',
        '',
        '// ── 직접 구현: 최솟값 선형 탐색 O(V²) ──',
        'int[] Dijkstra(Graph g, int start) {',
        '  var dist = Enumerable.Repeat(int.MaxValue, g.Size).ToArray();',
        '  var done = new bool[g.Size];',
        '  dist[start] = 0;',
        '  for (int iter = 0; iter < g.Size; iter++) {',
        '    int v = -1;',
        '    for (int i = 0; i < g.Size; i++) {',
        '      if (!done[i] && (v == -1 || dist[i] < dist[v])) v = i;',
        '    }',
        '    if (v == -1 || dist[v] == int.MaxValue) break;',
        '    done[v] = true;',
        '    foreach ((int next, int w) in g.Neighbors(v)) {',
        '      if (dist[v] + w < dist[next]) dist[next] = dist[v] + w;',
        '    }',
        '  }',
        '  return dist;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: heapq ──',
        'import heapq',
        '',
        'def dijkstra(graph, start):',
        '    dist = {start: 0}',
        '    pq = [(0, start)]',
        '    while pq:',
        '        d, v = heapq.heappop(pq)',
        '        if d > dist.get(v, float("inf")):',
        '            continue  # 이미 더 좋은 경로로 확정됨',
        '        for nxt, w in graph.neighbors(v):',
        '            nd = d + w',
        '            if nd < dist.get(nxt, float("inf")):',
        '                dist[nxt] = nd',
        '                heapq.heappush(pq, (nd, nxt))',
        '    return dist',
        '',
        '# ── 직접 구현: 최솟값 선형 탐색 O(V²) ──',
        'def dijkstra(graph, start):',
        '    dist = {start: 0}',
        '    done = set()',
        '    while True:',
        '        # 미확정 정점 중 최소 비용을 선형 탐색',
        '        v = min((u for u in dist if u not in done),',
        '                key=dist.get, default=None)',
        '        if v is None:',
        '            break',
        '        done.add(v)',
        '        for nxt, w in graph.neighbors(v):',
        '            nd = dist[v] + w',
        '            if nd < dist.get(nxt, float("inf")):',
        '                dist[nxt] = nd',
        '    return dist'
      ],
      java: [
        '// ── 표준 라이브러리: PriorityQueue ──',
        'int[] dijkstra(Graph g, int start) {',
        '  int[] dist = new int[g.size()];',
        '  Arrays.fill(dist, Integer.MAX_VALUE);',
        '  // 원소: {정점, 누적 비용}',
        '  PriorityQueue<int[]> pq =',
        '      new PriorityQueue<>((a, b) -> a[1] - b[1]);',
        '  dist[start] = 0;',
        '  pq.add(new int[]{start, 0});',
        '  while (!pq.isEmpty()) {',
        '    int[] cur = pq.poll();',
        '    int v = cur[0], d = cur[1];',
        '    if (d > dist[v]) continue; // 이미 확정됨',
        '    for (int[] e : g.neighbors(v)) {',
        '      int next = e[0], w = e[1];',
        '      if (dist[v] + w < dist[next]) {',
        '        dist[next] = dist[v] + w;',
        '        pq.add(new int[]{next, dist[next]});',
        '      }',
        '    }',
        '  }',
        '  return dist;',
        '}',
        '',
        '// ── 직접 구현: 최솟값 선형 탐색 O(V²) ──',
        'int[] dijkstra(Graph g, int start) {',
        '  int[] dist = new int[g.size()];',
        '  boolean[] done = new boolean[g.size()];',
        '  Arrays.fill(dist, Integer.MAX_VALUE);',
        '  dist[start] = 0;',
        '  for (int iter = 0; iter < g.size(); iter++) {',
        '    int v = -1;',
        '    for (int i = 0; i < g.size(); i++) {',
        '      if (!done[i] && (v == -1 || dist[i] < dist[v])) v = i;',
        '    }',
        '    if (v == -1 || dist[v] == Integer.MAX_VALUE) break;',
        '    done[v] = true;',
        '    for (int[] e : g.neighbors(v)) {',
        '      int next = e[0], w = e[1];',
        '      if (dist[v] + w < dist[next]) dist[next] = dist[v] + w;',
        '    }',
        '  }',
        '  return dist;',
        '}'
      ]
    }
  },
  {
    id: 'two-pointers',
    no: '011',
    title: 'Two Pointers',
    ko: '투 포인터',
    category: 'Searching',
    color: '#8fb8b0',
    complexity: 'O(n)',
    note: '정렬된 배열의 양 끝에서 좁혀 오며 합이 맞는 쌍을 찾는다',
    best: 'O(1)',
    worst: 'O(n)',
    tags: ['Sorted input', 'Pair search'],
    extras: [
      {
        module: 'two-pointers-vs-brute',
        title: '브루트포스와의 비교 횟수',
        caption: '모든 쌍을 검사하면 O(n²)이지만, 정렬된 배열에서는 포인터 두 개가 한 번씩만 움직여 O(n).'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: 해시셋 (정렬 불필요) ──',
        'bool twoSum(const std::vector<int>& a, int target) {',
        '  std::unordered_set<int> seen;',
        '  for (int x : a) {',
        '    if (seen.count(target - x)) return true;',
        '    seen.insert(x);',
        '  }',
        '  return false;',
        '}',
        '',
        '// ── 직접 구현: 투 포인터 (정렬된 배열) ──',
        'bool twoSum(const std::vector<int>& a, int target) {',
        '  int lo = 0, hi = (int)a.size() - 1;',
        '  while (lo < hi) {',
        '    int sum = a[lo] + a[hi];',
        '    if (sum == target) return true;',
        '    if (sum < target) lo++;',
        '    else hi--;',
        '  }',
        '  return false;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort 정렬 후 투 포인터 ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'int twoSum(int a[], int n, int target) {',
        '  qsort(a, n, sizeof(int), cmpAsc);',
        '  int lo = 0, hi = n - 1;',
        '  while (lo < hi) {',
        '    int sum = a[lo] + a[hi];',
        '    if (sum == target) return 1;',
        '    if (sum < target) lo++;',
        '    else hi--;',
        '  }',
        '  return 0;',
        '}',
        '',
        '// ── 직접 구현: 투 포인터 (정렬된 배열) ──',
        'int twoSum(const int a[], int n, int target) {',
        '  int lo = 0, hi = n - 1;',
        '  while (lo < hi) {',
        '    int sum = a[lo] + a[hi];',
        '    if (sum == target) return 1;',
        '    if (sum < target) lo++;',
        '    else hi--;',
        '  }',
        '  return 0;',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: HashSet (정렬 불필요) ──',
        'bool TwoSum(int[] a, int target) {',
        '  var seen = new HashSet<int>();',
        '  foreach (int x in a) {',
        '    if (seen.Contains(target - x)) return true;',
        '    seen.Add(x);',
        '  }',
        '  return false;',
        '}',
        '',
        '// ── 직접 구현: 투 포인터 (정렬된 배열) ──',
        'bool TwoSum(int[] a, int target) {',
        '  int lo = 0, hi = a.Length - 1;',
        '  while (lo < hi) {',
        '    int sum = a[lo] + a[hi];',
        '    if (sum == target) return true;',
        '    if (sum < target) lo++;',
        '    else hi--;',
        '  }',
        '  return false;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: 집합 (정렬 불필요) ──',
        'def two_sum(a, target):',
        '    seen = set()',
        '    for x in a:',
        '        if target - x in seen:',
        '            return True',
        '        seen.add(x)',
        '    return False',
        '',
        '# ── 직접 구현: 투 포인터 (정렬된 배열) ──',
        'def two_sum(a, target):',
        '    lo, hi = 0, len(a) - 1',
        '    while lo < hi:',
        '        s = a[lo] + a[hi]',
        '        if s == target:',
        '            return True',
        '        if s < target:',
        '            lo += 1',
        '        else:',
        '            hi -= 1',
        '    return False'
      ],
      java: [
        '// ── 표준 라이브러리: HashSet (정렬 불필요) ──',
        'boolean twoSum(int[] a, int target) {',
        '  Set<Integer> seen = new HashSet<>();',
        '  for (int x : a) {',
        '    if (seen.contains(target - x)) return true;',
        '    seen.add(x);',
        '  }',
        '  return false;',
        '}',
        '',
        '// ── 직접 구현: 투 포인터 (정렬된 배열) ──',
        'boolean twoSum(int[] a, int target) {',
        '  int lo = 0, hi = a.length - 1;',
        '  while (lo < hi) {',
        '    int sum = a[lo] + a[hi];',
        '    if (sum == target) return true;',
        '    if (sum < target) lo++;',
        '    else hi--;',
        '  }',
        '  return false;',
        '}'
      ]
    }
  },
  {
    id: 'sliding-window',
    no: '012',
    title: 'Sliding Window',
    ko: '슬라이딩 윈도우',
    category: 'Searching',
    color: '#c4b07f',
    complexity: 'O(n)',
    note: '창문을 한 칸씩 밀며 새 값만 더하고 빠진 값만 뺀다',
    best: 'O(n)',
    worst: 'O(n)',
    tags: ['Window', 'Running sum'],
    extras: [
      {
        module: 'sliding-window-naive',
        title: '매번 다시 합산 vs 슬라이딩',
        caption: '같은 결과를 내는 데 naive는 O(nk), 슬라이딩 윈도우는 O(n). 빠진 값을 빼고 새 값만 더하는 재사용이 핵심.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::accumulate ──',
        'int maxWindowSum(const vector<int>& a, int k) {',
        '  int sum = accumulate(a.begin(), a.begin() + k, 0);',
        '  int best = sum;',
        '  for (int i = k; i < (int)a.size(); i++) {',
        '    sum += a[i] - a[i - k];',
        '    best = max(best, sum);',
        '  }',
        '  return best;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int maxWindowSum(const vector<int>& a, int k) {',
        '  int sum = 0;',
        '  for (int i = 0; i < k; i++) sum += a[i];',
        '  int best = sum;',
        '  for (int i = k; i < (int)a.size(); i++) {',
        '    sum += a[i] - a[i - k];',
        '    if (sum > best) best = sum;',
        '  }',
        '  return best;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리 ──',
        '// C 표준에는 윈도우 합 함수가 없어 배열로 직접 작성',
        'int maxWindowSum(const int* a, int n, int k) {',
        '  int sum = 0;',
        '  for (int i = 0; i < k; i++) sum += a[i];',
        '  int best = sum;',
        '  for (int i = k; i < n; i++) {',
        '    sum += a[i] - a[i - k];',
        '    if (sum > best) best = sum;',
        '  }',
        '  return best;',
        '}',
        '',
        '// ── 직접 구현: naive O(nk) ──',
        'int maxWindowSum(const int* a, int n, int k) {',
        '  int best = 0;',
        '  for (int i = 0; i + k <= n; i++) {',
        '    int sum = 0;',
        '    for (int j = i; j < i + k; j++) sum += a[j];',
        '    if (i == 0 || sum > best) best = sum;',
        '  }',
        '  return best;',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: LINQ ──',
        'int MaxWindowSum(int[] a, int k) {',
        '  return Enumerable.Range(0, a.Length - k + 1)',
        '    .Select(i => a.Skip(i).Take(k).Sum())',
        '    .Max();',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int MaxWindowSum(int[] a, int k) {',
        '  int sum = 0;',
        '  for (int i = 0; i < k; i++) sum += a[i];',
        '  int best = sum;',
        '  for (int i = k; i < a.Length; i++) {',
        '    sum += a[i] - a[i - k];',
        '    if (sum > best) best = sum;',
        '  }',
        '  return best;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: 슬라이싱 + sum ──',
        'def max_window_sum(a, k):',
        '    return max(sum(a[i:i + k]) for i in range(len(a) - k + 1))',
        '',
        '# ── 직접 구현 ──',
        'def max_window_sum(a, k):',
        '    s = sum(a[:k])',
        '    best = s',
        '    for i in range(k, len(a)):',
        '        s += a[i] - a[i - k]',
        '        best = max(best, s)',
        '    return best'
      ],
      java: [
        '// ── 표준 라이브러리: IntStream ──',
        'int maxWindowSum(int[] a, int k) {',
        '  return IntStream.range(0, a.length - k + 1)',
        '    .map(i -> Arrays.stream(a, i, i + k).sum())',
        '    .max().getAsInt();',
        '}',
        '',
        '// ── 직접 구현 ──',
        'int maxWindowSum(int[] a, int k) {',
        '  int sum = 0;',
        '  for (int i = 0; i < k; i++) sum += a[i];',
        '  int best = sum;',
        '  for (int i = k; i < a.length; i++) {',
        '    sum += a[i] - a[i - k];',
        '    if (sum > best) best = sum;',
        '  }',
        '  return best;',
        '}'
      ]
    }
  },
  {
    id: 'heap-sort',
    no: '013',
    title: 'Heap Sort',
    ko: '힙 정렬',
    category: 'Sorting',
    color: '#b49ac8',
    complexity: 'O(n log n)',
    best: 'O(n log n)',
    worst: 'O(n log n)',
    note: '최대 힙을 만들고, 루트의 최댓값을 하나씩 뽑아 뒤부터 채운다',
    tags: ['Heap', 'In-place'],
    extras: [
      {
        module: 'heap-sort-heapify',
        title: 'sift-down 확대',
        caption: '힙 성질이 깨진 노드는 자식 중 큰 값과 자리를 바꾸며 가라앉는다. 이 sift-down이 힙 정렬 전체의 기본 동작.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리: std::make_heap + sort_heap ──',
        'void heapSort(vector<int>& a) {',
        '  make_heap(a.begin(), a.end());   // 최대 힙 구성',
        '  sort_heap(a.begin(), a.end());   // 루트를 뽑으며 정렬',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void siftDown(vector<int>& a, int n, int i) {',
        '  while (true) {',
        '    int l = 2 * i + 1, r = 2 * i + 2, m = i;',
        '    if (l < n && a[l] > a[m]) m = l;',
        '    if (r < n && a[r] > a[m]) m = r;',
        '    if (m == i) break;',
        '    swap(a[i], a[m]);',
        '    i = m;',
        '  }',
        '}',
        '',
        'void heapSort(vector<int>& a) {',
        '  int n = a.size();',
        '  for (int i = n / 2 - 1; i >= 0; i--) siftDown(a, n, i);',
        '  for (int end = n - 1; end > 0; end--) {',
        '    swap(a[0], a[end]);',
        '    siftDown(a, end, 0);',
        '  }',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리 ──',
        '// C 표준에는 힙이 없어 정렬은 보통 qsort를 사용',
        'int cmp(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void sort(int* a, int n) {',
        '  qsort(a, n, sizeof(int), cmp);',
        '}',
        '',
        '// ── 직접 구현: 힙 정렬 ──',
        'void siftDown(int* a, int n, int i) {',
        '  while (1) {',
        '    int l = 2 * i + 1, r = 2 * i + 2, m = i;',
        '    if (l < n && a[l] > a[m]) m = l;',
        '    if (r < n && a[r] > a[m]) m = r;',
        '    if (m == i) break;',
        '    int t = a[i]; a[i] = a[m]; a[m] = t;',
        '    i = m;',
        '  }',
        '}',
        '',
        'void heapSort(int* a, int n) {',
        '  for (int i = n / 2 - 1; i >= 0; i--) siftDown(a, n, i);',
        '  for (int end = n - 1; end > 0; end--) {',
        '    int t = a[0]; a[0] = a[end]; a[end] = t;',
        '    siftDown(a, end, 0);',
        '  }',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: PriorityQueue ──',
        'int[] HeapSort(int[] a) {',
        '  var pq = new PriorityQueue<int, int>();',
        '  foreach (var x in a) pq.Enqueue(x, x);',
        '  var result = new int[a.Length];',
        '  for (int i = 0; i < result.Length; i++)',
        '    result[i] = pq.Dequeue();',
        '  return result;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void SiftDown(int[] a, int n, int i) {',
        '  while (true) {',
        '    int l = 2 * i + 1, r = 2 * i + 2, m = i;',
        '    if (l < n && a[l] > a[m]) m = l;',
        '    if (r < n && a[r] > a[m]) m = r;',
        '    if (m == i) break;',
        '    (a[i], a[m]) = (a[m], a[i]);',
        '    i = m;',
        '  }',
        '}',
        '',
        'void HeapSort(int[] a) {',
        '  int n = a.Length;',
        '  for (int i = n / 2 - 1; i >= 0; i--) SiftDown(a, n, i);',
        '  for (int end = n - 1; end > 0; end--) {',
        '    (a[0], a[end]) = (a[end], a[0]);',
        '    SiftDown(a, end, 0);',
        '  }',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: heapq ──',
        'import heapq',
        '',
        'def heap_sort(a):',
        '    h = a[:]',
        '    heapq.heapify(h)  # 최소 힙 구성',
        '    return [heapq.heappop(h) for _ in range(len(h))]',
        '',
        '# ── 직접 구현 ──',
        'def sift_down(a, n, i):',
        '    while True:',
        '        l, r, m = 2 * i + 1, 2 * i + 2, i',
        '        if l < n and a[l] > a[m]: m = l',
        '        if r < n and a[r] > a[m]: m = r',
        '        if m == i: break',
        '        a[i], a[m] = a[m], a[i]',
        '        i = m',
        '',
        'def heap_sort(a):',
        '    n = len(a)',
        '    for i in range(n // 2 - 1, -1, -1):',
        '        sift_down(a, n, i)',
        '    for end in range(n - 1, 0, -1):',
        '        a[0], a[end] = a[end], a[0]',
        '        sift_down(a, end, 0)',
        '    return a'
      ],
      java: [
        '// ── 표준 라이브러리: PriorityQueue ──',
        'int[] heapSort(int[] a) {',
        '  PriorityQueue<Integer> pq = new PriorityQueue<>();',
        '  for (int x : a) pq.offer(x);',
        '  int[] result = new int[a.length];',
        '  for (int i = 0; i < result.length; i++)',
        '    result[i] = pq.poll();',
        '  return result;',
        '}',
        '',
        '// ── 직접 구현 ──',
        'void siftDown(int[] a, int n, int i) {',
        '  while (true) {',
        '    int l = 2 * i + 1, r = 2 * i + 2, m = i;',
        '    if (l < n && a[l] > a[m]) m = l;',
        '    if (r < n && a[r] > a[m]) m = r;',
        '    if (m == i) break;',
        '    int t = a[i]; a[i] = a[m]; a[m] = t;',
        '    i = m;',
        '  }',
        '}',
        '',
        'void heapSort(int[] a) {',
        '  int n = a.length;',
        '  for (int i = n / 2 - 1; i >= 0; i--) siftDown(a, n, i);',
        '  for (int end = n - 1; end > 0; end--) {',
        '    int t = a[0]; a[0] = a[end]; a[end] = t;',
        '    siftDown(a, end, 0);',
        '  }',
        '}'
      ]
    }
  },
  {
    id: 'counting-sort',
    no: '014',
    title: 'Counting Sort',
    ko: '계수 정렬',
    category: 'Sorting',
    color: '#a8c4b0',
    complexity: 'O(n + k)',
    best: 'O(n + k)',
    worst: 'O(n + k)',
    note: '값의 개수를 세고 누적 합으로 위치를 정해 한 번에 배치한다',
    tags: ['Non-comparison', 'Stable'],
    extras: [
      {
        module: 'counting-sort-stable',
        title: '같은 값의 순서가 유지되는 이유',
        caption: '출력을 뒤에서부터 채우면 같은 값끼리는 입력 순서가 그대로 유지된다. 계수 정렬이 안정 정렬인 핵심.'
      }
    ],
    code: {
      cpp: [
        '// ── 표준 라이브러리 활용 ──',
        'void countingSort(vector<int>& a) {',
        '  int k = *max_element(a.begin(), a.end());',
        '  vector<int> cnt(k + 1, 0);',
        '  for (int x : a) cnt[x]++;',
        '  int i = 0;',
        '  for (int v = 0; v <= k; v++)',
        '    fill_n(a.begin() + i, cnt[v], v), i += cnt[v];',
        '}',
        '',
        '// ── 직접 구현 (안정 버전) ──',
        'void countingSort(vector<int>& a) {',
        '  int k = a[0];',
        '  for (int x : a) if (x > k) k = x;',
        '  vector<int> cnt(k + 1, 0);',
        '  for (int x : a) cnt[x]++;',
        '  for (int v = 1; v <= k; v++) cnt[v] += cnt[v - 1];',
        '  vector<int> out(a.size());',
        '  for (int i = (int)a.size() - 1; i >= 0; i--)',
        '    out[--cnt[a[i]]] = a[i];',
        '  a = out;',
        '}'
      ],
      c: [
        '// ── 표준 라이브러리: qsort (stdlib.h) ──',
        'int cmpAsc(const void* x, const void* y) {',
        '  return *(const int*)x - *(const int*)y;',
        '}',
        '',
        'void sort(int* a, int n) {',
        '  qsort(a, n, sizeof(int), cmpAsc); // 실무에서는 내장 정렬',
        '}',
        '',
        '// ── 직접 구현: 계수 정렬 (안정 버전) ──',
        'void countingSort(int* a, int n, int k) {',
        '  int* cnt = calloc(k + 1, sizeof(int));',
        '  int* out = malloc(n * sizeof(int));',
        '  for (int i = 0; i < n; i++) cnt[a[i]]++;',
        '  for (int v = 1; v <= k; v++) cnt[v] += cnt[v - 1];',
        '  for (int i = n - 1; i >= 0; i--) out[--cnt[a[i]]] = a[i];',
        '  memcpy(a, out, n * sizeof(int));',
        '  free(cnt); free(out);',
        '}'
      ],
      csharp: [
        '// ── 표준 라이브러리: LINQ ──',
        'int[] CountingSort(int[] a) {',
        '  return a.GroupBy(x => x)',
        '    .OrderBy(g => g.Key)',
        '    .SelectMany(g => g)',
        '    .ToArray();',
        '}',
        '',
        '// ── 직접 구현 (안정 버전) ──',
        'int[] CountingSort(int[] a) {',
        '  int k = a.Max();',
        '  var cnt = new int[k + 1];',
        '  foreach (var x in a) cnt[x]++;',
        '  for (int v = 1; v <= k; v++) cnt[v] += cnt[v - 1];',
        '  var outArr = new int[a.Length];',
        '  for (int i = a.Length - 1; i >= 0; i--)',
        '    outArr[--cnt[a[i]]] = a[i];',
        '  return outArr;',
        '}'
      ],
      python: [
        '# ── 표준 라이브러리: collections.Counter ──',
        'from collections import Counter',
        '',
        'def counting_sort(a):',
        '    cnt = Counter(a)',
        '    return [v for v in range(max(a) + 1) for _ in range(cnt[v])]',
        '',
        '# ── 직접 구현 (안정 버전) ──',
        'def counting_sort(a):',
        '    k = max(a)',
        '    cnt = [0] * (k + 1)',
        '    for x in a:',
        '        cnt[x] += 1',
        '    for v in range(1, k + 1):',
        '        cnt[v] += cnt[v - 1]',
        '    out = [0] * len(a)',
        '    for x in reversed(a):',
        '        cnt[x] -= 1',
        '        out[cnt[x]] = x',
        '    return out'
      ],
      java: [
        '// ── 표준 라이브러리: 스트림 ──',
        'int[] countingSort(int[] a) {',
        '  return Arrays.stream(a).sorted().toArray();',
        '}',
        '',
        '// ── 직접 구현 (안정 버전) ──',
        'int[] countingSort(int[] a) {',
        '  int k = Arrays.stream(a).max().getAsInt();',
        '  int[] cnt = new int[k + 1];',
        '  for (int x : a) cnt[x]++;',
        '  for (int v = 1; v <= k; v++) cnt[v] += cnt[v - 1];',
        '  int[] out = new int[a.length];',
        '  for (int i = a.length - 1; i >= 0; i--)',
        '    out[--cnt[a[i]]] = a[i];',
        '  return out;',
        '}'
      ]
    }
  }
];
